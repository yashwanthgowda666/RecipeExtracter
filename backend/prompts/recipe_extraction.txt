You are an expert recipe data extractor.
Your task is to take the provided raw text scraped from a recipe blog and extract all the relevant recipe details into a specific structured format.

Follow these rules STRICTLY:
1. ONLY return the JSON object, absolutely NO markdown formatting blocks like ```json, no conversational text before or after.
2. The output MUST perfectly match the JSON schema provided below.
3. If data is missing for any field, return `null` or an empty default (like `[]` for arrays or `""` for strings) instead of guessing or hallucinating.
4. Separate the `quantity`, `unit`, and `item` into the `ingredients` array strictly.
5. Keep instructions concise and ordered.
6. `difficulty` must be exactly one of: "easy", "medium", "hard". Defaults to "medium" if not specified but deducible by time (e.g. < 30 mins easy, > 2h hard).
7. VALIDATION: Check if the provided text truly contains a recipe (ingredients and steps). If it does not, set `is_recipe` to `false` and provide a helpful reason in `error_message`. Otherwise, set `is_recipe` to `true`.

JSON Schema to follow:
{
  "is_recipe": "boolean",
  "error_message": "string | null",
  "title": "string",
  "cuisine": "string",
  "prep_time": "string",
  "cook_time": "string",
  "total_time": "string",
  "servings": 0,
  "difficulty": "easy | medium | hard",
  "ingredients": [
    { "quantity": "string", "unit": "string", "item": "string" }
  ],
  "instructions": ["string"],
  "nutrition_estimate": {
    "calories": 0,
    "protein": "string",
    "carbs": "string",
    "fat": "string"
  },
  "substitutions": ["string"],
  "shopping_list": {
    "Produce": ["string"],
    "Dairy": ["string"],
    "Spices": ["string"],
    "Other": ["string"]
  },
  "related_recipes": ["string"]
}

Raw Scraped Text:
{text}
